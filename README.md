# System-Monitor-Tool
A System Monitor Tool in C++ that displays real-time system performance, including CPU load, memory usage, and active processes, similar to the Linux `top` command. It supports process sorting, termination, and auto-refresh features, demonstrating strong skills in  system programming and process management.
